apiVersion: kaptain.org/1.7
kind: layer-and-layerset-build
metadata:
  description: "Common docker build settings for image-environment-base images"

spec:
  layers:
    - layerset-and-layer-build:[1.3,2.0)
